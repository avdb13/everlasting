fn get_pins(observed: &str) -> Vec<String> {
    let grid = [
        [Some(1), Some(2), Some(3)],
        [Some(4), Some(5), Some(6)],
        [Some(7), Some(8), Some(9)],
        [None, Some(0), None],
    ];

    let v: Vec<_> = observed.chars().collect();
}
